services:
  - type: web
    name: tw-stock-radar
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: LIVE_MODE
        value: "true"
      - key: FUGLE_API_KEY
        sync: false
